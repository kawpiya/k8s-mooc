apiVersion: v1
kind: Service
metadata:
  name: ping-pong-app-svc
spec:
  type: ClusterIP
  selector:
    app: ping-pong-app # This is the app as declared in the deployment.
  ports:
    - port: 80
      protocol: TCP
      targetPort: 9090