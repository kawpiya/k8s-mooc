apiVersion: v1
kind: ConfigMap
metadata:
  name: pg-init-sql
  namespace: project
data:
  init.sql: |
    CREATE TABLE IF NOT EXISTS todos (
      id SERIAL PRIMARY KEY,
      title TEXT NOT NULL
    );
